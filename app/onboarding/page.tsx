// Onboarding page route — single page, step-based internal state
import { OnboardingPage } from "@/features/onboarding/components/onboarding-page";

export default function Page() {
  return <OnboardingPage />;
}
